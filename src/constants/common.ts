export const PLATFORM = 'web3neo';

export enum ArgumentDataType {
  STRING = 'string',
  BOOLEAN = 'boolean',
  HASH160 = 'hash160',
  INTEGER = 'integer',
  BYTEARRAY = 'bytearray',
  ARRAY = 'array',
};
