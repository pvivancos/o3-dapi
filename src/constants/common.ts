export const PLATFORM = 'web3neo';
export const VERSION = 'v1';

export enum ArgumentDataType {
  STRING = 'string',
  BOOLEAN = 'boolean',
  HASH160 = 'hash160',
  INTEGER = 'integer',
  BYTEARRAY = 'bytearray',
  ARRAY = 'array',
}
